const TestimonialsComponent={render:(content,layout,colorMode,pStyle,lang)=>{const items=SectionUtils.lines(content);if(!items.length)return'';const t=SectionUtils.theme(colorMode,pStyle);const title=lang==='th'?'คำรับรอง':'Testimonials';const body=items.map(l=>{const [quote,name,role]=SectionUtils.parts(l);return `<blockquote class="extra-card testimonial"><p>“${quote||''}”</p>${name?`<footer>${name}${role?` — ${role}`:''}</footer>`:''}</blockquote>`}).join('');return SectionUtils.wrap(SectionUtils.title(title,t)+`<div class="extra-grid">${body}</div>`,'testimonials-section')}};

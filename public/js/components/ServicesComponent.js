const ServicesComponent={render:(content,layout,colorMode,pStyle,lang)=>{const items=SectionUtils.lines(content);if(!items.length)return'';const t=SectionUtils.theme(colorMode,pStyle);const title=lang==='th'?'บริการ / สิ่งที่ช่วยได้':'Services';const body=items.map(l=>{const [name,desc]=SectionUtils.parts(l);return `<div class="extra-card compact"><h4>${name||''}</h4>${desc?`<p>${desc}</p>`:''}</div>`}).join('');return SectionUtils.wrap(SectionUtils.title(title,t)+`<div class="extra-grid three">${body}</div>`,'services-section')}};

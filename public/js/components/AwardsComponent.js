const AwardsComponent={render:(content,layout,colorMode,pStyle,lang)=>{const items=SectionUtils.lines(content);if(!items.length)return'';const t=SectionUtils.theme(colorMode,pStyle);const title=lang==='th'?'รางวัลและความสำเร็จ':'Awards & Achievements';const body=items.map(l=>{const [name,org,year,desc]=SectionUtils.parts(l);return `<div class="extra-card"><div class="extra-meta">${year||''}</div><h4>${name||''}</h4>${org?`<p class="extra-sub">${org}</p>`:''}${desc?`<p>${desc}</p>`:''}</div>`}).join('');return SectionUtils.wrap(SectionUtils.title(title,t)+`<div class="extra-grid">${body}</div>`,'awards-section')}};

V23 Theme Wheel Alignment Fix

Changes:
- Keeps the theme picker at the middle-right of the viewport.
- Centers the up/down buttons, wheel scroll area, active swatch, and label inside the picker component.
- Removes layout behavior where internal controls visually align to the page rather than the component.
- Keeps drag, wheel-scroll, and click-to-center behavior from v22.

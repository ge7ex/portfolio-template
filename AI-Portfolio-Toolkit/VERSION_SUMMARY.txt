V42 Mobile Scroll + Studio Data Fix

Changed files:
- index.html
- css/style.css
- js/storage.js
- js/components/PrintDesigner.js
- js/components/PrintDesignerHotfix.js

Fixes:
1. Mobile/tablet scrollytelling now uses vertical page scroll to expand/collapse the project image rail and move images, matching desktop behavior. It is no longer a static grid or manual horizontal swipe gallery.
2. PDF Layout Studio no longer treats a portfolio as demo-only just because name fields still contain placeholders. Real project text/images are preserved.
3. Studio header preview uses a transparent header layer on the themed PDF header, so text is not hidden by a white object box.
4. PrintDesigner script cache version bumped to V42.
5. Print PDF landscape remains hidden/disabled by the existing PrintDesigner/export guard.

Install:
Replace the files above in AI-Portfolio-Toolkit, or upload the complete package.
